terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.16"
    }
  }
}

provider "aws" {
  profile = "${PROJECT_NAME}_terraform"
  region  = "${REGION}"
}
